{
  "name": "msbios/deploy",
  "description": "MSBios Deploy",
  "license": "GPL",
  "keywords": [
    "php"
  ],
  "homepage": "https://github.com/msbios/msbios-deploy",
  "minimum-stability": "stable",
  "authors": [
    {
      "name": "Judzhin Miles",
      "email": "info@msbios.com"
    }
  ],
  "autoload": {
    "psr-4": {
      "MSBios\\Deploy\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "MSBiosTest\\Deploy\\": "tests/"
    }
  },
  "require": {
    "msbios/monolog": "~1.0",
    "deployer/deployer": "^6.0"
  },
  "require-dev": {
    "phpunit/phpunit": "^5.7",
    "zendframework/zend-test": "^3.1",
    "squizlabs/php_codesniffer": "^3.0",
    "msbios/application": "^1.0"
  },
  "scripts": {
    "cs-check": "phpcs",
    "cs-fix": "phpcbf",
    "test": [
      "@cs-check",
      "phpunit --colors=always"
    ],
    "serve": "php -S 0.0.0.0:3107 -t bin bin/msbios-deploy-module.php",
    "dep": "./vendor/bin/dep",
    "commit": "git add . && git commit -a -m \"Lazy Composer Commiter\" && git push",
    "cache":[
      "rm -Rf data/cache/*.php",
      "rm -Rf data/cache/*.dat",
      "rm -Rf data/logs/*.log"
    ],
    "test-coverage": "phpunit --colors=always --coverage-clover clover.xml",
    "upload-coverage": "coveralls -v"
  },
  "extra": {
    "zf": {
      "component": "MSBios\\Deploy",
      "config-provider": "MSBios\\Deploy\\ConfigProvider"
    }
  }
}
